{
  "name": "forescore-ios",
  "version": "1.0.0",
  "description": "ForeScore Golf Payout Calculator - iOS App",
  "main": "index.js",
  "scripts": {
    "cap:add-ios": "npx cap add ios",
    "cap:copy": "npx cap copy ios",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
