/**
 * ForeScore Application Version
 * 
 * IMPORTANT: When updating this version for deployment:
 * 1. Update the version here
 * 2. Update the matching version in client/public/sw.js (line 2)
 * 
 * Both must match for cache invalidation to work correctly.
 * See DEPLOYMENT.md for full deployment instructions.
 */
export const APP_VERSION = '1.0.0';
